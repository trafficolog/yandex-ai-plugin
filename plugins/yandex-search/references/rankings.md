# Rankings and competitors

Compare rank snapshots only when `config_fingerprint` matches. Rank delta is `before_rank - after_rank`, so a move from #4 to #2 is `+2`. Competitor metrics are query presence, top-3/top-10 presence, median best rank and URLs found. Call the proportion `SERP presence rate`; never call it market share.
