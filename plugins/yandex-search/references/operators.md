# Search operators

Yandex search supports context operators such as `-`, `!`, `+`, quoted phrases and OR, plus advanced filters such as `site:`, `host:`, `url:`, `domain:`, `mime:`, `lang:` and `date:`. Preserve the exact query expression in artifacts because operators change the result set.
