# Safety

Yandex Marketing 1.0.0 is read/analyze/reconcile/recommend/preview only. It contains no transport or credential layer and never performs live campaign, budget, keyword, strategy or goal writes. Consequential recommendations are delegated to the owning service plugin, which must enforce its own preview and explicit approval flow.
